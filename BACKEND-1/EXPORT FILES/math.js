const Sum = (a,b) => a+b;
const diff = (a,b) =>a-b;
const mul = (a,b) =>a*b;
const PI= 3.14;

module.exports = {
   Sum : sum,
   Subs : diff,
   Mul : mul,
   PI: PI,
};

/*math.exports.sum = (a,b) => a+b */